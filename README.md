# LeetCode Proxy Server

Proxy server for DV DSA Tracker to fetch LeetCode data.

## Deploy on Railway

1. Push to GitHub
2. Connect to Railway
3. Deploy — done!

## Endpoints

- `GET /` — health check
- `GET /leetcode/:username` — fetch all solved problems
